bvlc_alexnet.caffemodel
	- a pretrained model (trained on ImageNet) used to initialize the network weights

imagenet_mean.binaryproto
	- the computed average image of the ImageNet dataset.
	- required for zero-centering of the input images.